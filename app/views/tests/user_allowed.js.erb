$('.cancel-button').click();

$('#created_tests').empty().append('<%= j render "shared/content_table", content: current_user.tests.page(params[:page]), model_name: "tests" %>');

$('.information').popover();
$('.btn, .action').tooltip();